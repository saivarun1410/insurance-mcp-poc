{
  "name": "insurance-mcp-poc",
  "version": "0.1.0",
  "description": "An MCP server exposing life-insurance policy documents, applications, and product rules as agent tools.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "db:up": "(docker compose up -d || docker-compose up -d) && node scripts/wait-for-db.mjs",
    "db:down": "docker compose down -v || docker-compose down -v",
    "seed": "node scripts/seed.mjs",
    "start": "node src/index.js",
    "smoke": "node scripts/smoke.mjs",
    "demo": "node scripts/demo.mjs",
    "setup": "npm run db:up && npm run seed && npm run smoke"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "pg": "^8.13.0",
    "zod": "^3.23.0"
  }
}
